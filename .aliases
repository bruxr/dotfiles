# Easier navigation
alias ..="cd .."
alias ~="cd ~"
alias p="cd ~/Projects"

# Git shortcuts
alias ga="git add ."
alias gs="git status"
alias gp="git push"
alias gfu="git fetch upstream"
alias gc="git commit"
alias gca="git commit --amend"

# Easily launch servers
alias serve="http-server"
alias pgs="pg_ctl -D /usr/local/var/postgres start"
alias pgx="pg_ctl -D /usr/local/var/postgres stop"
alias mysqls="mysql.server start"
alias redis="redis-server /usr/local/etc/redis.conf"
alias mongodb="mongod --config /usr/local/etc/mongod.conf"

# Quickly launch a PHP dev server
alias phpsrv="php -S localhost:8000 -t public/"
